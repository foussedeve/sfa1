{% extends 'base.ecole.html.twig' %}

{% block title %}Ajoute les notes d'une classe !{% endblock %}

{% block body %}
<div class="row">
    <div class="card">

        <div class="card-body">
           
            <h4 class="">Ajouter un nouveau parent</h4>
             {{form(form)}}

        {# {{form_start(form)}}
            
        {% for formRow in form %}
         {{form_row(formRow)}}

        {% endfor %}
     

    <button type="submit" class="btn btn-primary bg-info  mr-2">Continuer</button>
    <button type="reset" class="btn btn-light">Cancel</button>
    {{form_end(form)}}  #}
</div>

</div>
</div>
{% endblock %}